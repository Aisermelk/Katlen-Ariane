name = "katlen-admin"
main = "worker.js"
compatibility_date = "2026-08-01"

kv_namespaces = [
  { binding = "SITE_KV", id = "COLOQUE_AQUI_O_ID_DO_NAMESPACE" }
]

# Secrets (NÃO vão neste arquivo — defina via `wrangler secret put`):
#   SESSION_SECRET
#   SETUP_TOKEN
